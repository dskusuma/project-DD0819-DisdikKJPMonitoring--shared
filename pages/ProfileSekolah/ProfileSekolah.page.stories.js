import React from 'react';
import { storiesOf } from '@storybook/react-native';
import ProfileSekolah from './ProfileSekolah.page';

storiesOf('PAGE Profile Sekolah', module)
  .add('default', () => <ProfileSekolah />)